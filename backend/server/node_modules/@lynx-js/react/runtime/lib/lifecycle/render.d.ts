import type { ComponentChild, ContainerNode } from 'preact';
declare function renderMainThread(): void;
declare function renderBackground(vnode: ComponentChild, parent: ContainerNode): void;
export { renderMainThread, renderBackground };
